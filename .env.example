# Directory containing your repos
REPOS_DIR="$HOME/repos"

# Agent used by the generic launcher alias when no agent argument is passed.
DEFAULT_AGENT="pi"

# Optional: override the exact launch command for the generic alias.
# LAUNCH_COMMAND="pi"
